[package]
name = "guion-fisica"
version = "0.1.0"
description = "Verified physics for físico buen físico pieces (ENCARGO / FISICA.md)"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
guion-fisica = { path = "." }
