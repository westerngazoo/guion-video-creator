[package]
name = "guion-brand"
version = "0.1.0"
description = "Themes, palettes, and post-fx for guion (M3)"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
motoreel = { workspace = true }
# R-0009: el registro de caras que `PpmSink::with_fonts` pide.
motoreel-typeset = { path = "../../../motoreel/crates/motoreel-typeset" }
serde = { workspace = true }
toml = { workspace = true }
