{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core plugins and playground invoke commands",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
